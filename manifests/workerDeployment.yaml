apiVersion: apps/v1
kind: Deployment
metadata:
  name: worker
  labels:
    app: worker
spec:
  replicas: 3
  selector:
    matchLabels:
      app: worker
  template:
    metadata:
      labels:
        app: worker
    spec:
      nodeSelector:
        role: mainapp 
      initContainers:
        - name: wait-for-postgres
          image: busybox:1.36
          command:
            - sh
            - -c
            - |
              echo "Waiting for Postgres to be ready..."
              until nc -z postgres-0.postgres-headless.default.svc.cluster.local 5432; do
                echo "Postgres not ready, retrying..."
                sleep 2
              done
              echo "Postgres is ready!"
        - name: wait-for-redis
          image: busybox:1.36
          command:
            - sh
            - -c
            - |
              echo "Waiting for Redis to be ready..."
              until nc -z redis-0.redis-headless.default.svc.cluster.local 6379; do
                echo "Redis not ready, retrying..."
                sleep 2
              done
              echo "Redis is ready!"
      containers:
        - name: worker
          image: neerajdeveloper/worker:latest
          env:
            - name: REDIS
              value: redis-0.redis-headless.default.svc.cluster.local
            - name: POSTGRES
              value: postgres-0.postgres-headless.default.svc.cluster.local
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
      restartPolicy: Always

---
apiVersion: v1
kind: Service
metadata:
  name: worker-service
spec:
  selector:
    app: worker
  ports:
    - port: 8080
      targetPort: 8080

